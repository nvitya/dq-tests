{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "dq: build active file",
      "type": "shell",
      "command": "${workspaceFolder}/../dq-lang/build/dq-comp",
      "args": [
        "-g",
        "-O0",
        "${file}",
        "-o",
        "${fileDirname}/${fileBasenameNoExtension}"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": []
    }
  ]
}
